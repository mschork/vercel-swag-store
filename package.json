{
  "name": "vercel-swag-store",
  "private": true,
  "packageManager": "pnpm@12.4.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "verify": "turbo run lint typecheck build test",
    "prepare": "husky"
  },
  "devDependencies": {
    "@sanity/blueprints": "^0.26.1",
    "husky": "^9.1.7",
    "lint-staged": "^17.5.1",
    "turbo": "^2.10.12"
  }
}
